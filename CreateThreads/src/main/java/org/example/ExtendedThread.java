package org.example;

public class ExtendedThread {
    public static void main(String[] args) {
        //create an instance of mythread
        MyThread myThread = new MyThread();
        //start the thread
        myThread.start(); //thread claz ekenma enne
    }
}
